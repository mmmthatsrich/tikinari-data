# Session Progress

**Update this file at the end of every session** — mark status, note row counts or key outcomes, and check off dependencies as they clear.

| # | Session | Status | Depends On |
|---|---------|--------|------------|
| 1 | Schema & project scaffold | ✓ Done | — |
| 2 | Williams download (14 HTML files) | ✓ Done | 1 |
| 3 | Williams parse → JSON (11,910 entries) | ✓ Done | 2 |
| 4 | Williams import → DB | ✓ Done | 3 |
| 5 | Papakupu PDF extraction → JSON (3,994 entries) | ✓ Done | 1 |
| 6 | Papakupu import → DB | ✓ Done | 5 |
| 6b | Papakupu website gap analysis + update — archived A/K/P sections from Wayback Machine; 9 missing headwords + 680 WRRT-TAPEHA terminology entries added; abbreviations expanded → papakupu_sources.json; DB now 4,683 entries | ✓ Done | 6 |
| 7 | POLLEX Māori scraper — Phase 1 (listing pages) + Phase 2 (2,931 entry pages) | ✓ Done | 1 |
| 8 | POLLEX import → DB (3,424 entries) | ✓ Done | 7 |
| 9 | Te Aka scraper — write script | ✓ Done | 1 |
| 9-run | Te Aka scraper — run (~46 hrs, 55,000 IDs) — 47,955 valid / 7,005 not found / 40 errors; completed | ✓ Done | 9 |
| 10 | Te Aka parse + import — 47,878 entries (47,955 valid IDs; 77 skipped with no parseable word-def); 100% have definitions, 77.3% usage examples, 58.6% audio, 41.2% citations; compute_content_hash fixed for list-of-dicts (synonyms field) | ✓ Done | 9-run |
| 11a | He Pātaka Kupu scraper — write script | ✓ Done | 1 |
| 11b | He Pātaka Kupu scraper — run (14,996 files downloaded) | ✓ Done | 11a |
| 12 | He Pātaka Kupu import — 24,941 sense records (12,638 words × avg 1.97 senses); 2,358 "no word found" pages skipped; schema extended with sense_number, synonyms, semantic_domain | ✓ Done | 11b |
| 13a | Paekupu scraper — write script (16,490 slugs found) | ✓ Done | 1 |
| 13b | Paekupu scraper — run (16,487 / 16,490 files; 3 slugs unreachable/404) | ✓ Done | 13a |
| 14 | Paekupu import — 16,486 entries (16,490 slugs; 3 unreachable, 1 URL-encoded slug skipped); schema extended with slug, definition_mi, audio_url, subject_areas, alternative_words, pos_mi; 99.7% have audio, 42.2% have alternative words | ✓ Done | 13b |
| 15 | Personal lexicon CRUD module — `scripts/05_personal_lexicon_crud.py` (`PersonalLexicon` class: add/get/update/delete/search/list_by_tag/list_all_tags/export_json/import_json/cross_source_lookup); `tests/test_personal_lexicon.py` (18 tests, all pass) | ✓ Done | 1 |
| 16 | Update tracking & maintenance tools — `scripts/06_update_tracker.py` (stats/vacuum/export subcommands) + `scripts/06_fts_rebuild.py`; stats shows meta vs actual counts for all 10 sources + 5 aux tables; vacuum compacts DB (78->75.6 MB) + integrity_check ok; FTS rebuild covers all 7 FTS tables | ✓ Done | 8, 10, 12, 14, 15 |
| 17 | Full test suite — `tests/test_full_suite.py` (93 tests: normalisation, schema, row counts, data quality, FTS, search normalisation, source metadata, etymology tables, source abbreviations, refresh tracking, performance); all pass in 0.6s | ✓ Done | 16 |
| 18 | POLLEX cognatesets + reflexes import → DB (2,931 cognatesets, 49,760 reflexes) | ✓ Done | 7 |
| 19 | LPO import → DB (2,820 rows) | ✓ Done | — |
| 20 | ACD import → DB (10,857 rows) | ✓ Done | — |
| 21 | Etymology linker (POLLEX → LPO → ACD) — 229 links (172 ACD-only, 45 LPO-only, 12 both); 3-tier matching (form-key + gloss validation + explicit note citations); `scripts/08_etymology_linker.py`; 7 new tests added (100 total, all pass) | ✓ Done | 18, 19, 20 |
| 22 | POLLEX level_name data quality fix — CC, CK, FU, LO, NO, RO, TO, TU | ✓ Done | 18 |
| 23 | Historical PDF assessment — Maunsell 5 pages + He Karao 4 pages OCR'd; quality acceptable; no tessdata_best or deskewing needed; He Karao p14 = syllable index (vocabulary starts p15+); samples in sources/{maunsell,hekarao}/samples/ | ✓ Done | — |
| 24a | Maunsell OCR — pages 145–244 — 100 files in sources/maunsell/ocr/; consistent PDF-15=book offset; ~1,200–1,600 chars/page; Otsu+UnsharpMask preprocessing | ✓ Done | 23 |
| 24b | He Karao OCR — pages 15–27 + 50–58 (p14=index, skip), image_to_data() TSV output, sources/hekarao/ocr/ — 22 TSV files; p15 top = syllable index remainder, vocabulary starts ~halfway down; ~163–223 words/page | ✓ Done | 23 |
| 25 | Parse OCR output → JSON — maunsell_entries.json (2,067 entries; curly-quote normalisation fix); hekarao_entries.json (690 entries, 300 no-gloss due to OCR quality; CSV QUOTE_NONE fix for embedded-quote bug) | ✓ Done | 24a, 24b |
| 25c | Maunsell + He Karao curation — CSVs exported (2,067 Maunsell + 690 He Karao rows); manually assign modern Māori spellings (with macrons) in spreadsheet; run --import-csv to push decisions back to JSON; only entries with modern_headword filled in are imported into DB in session 26 | ⏸ Skipped | 25 |
| 26 | Schema additions (maunsell_entries, hekarao_entries tables) + import scripts — import only curated entries (modern_headword set); store original_headword for provenance | ⏸ Skipped | 25c |
| 27 | Test suite gap-fill — 2 new tests (test_paekupu_content_hash_populated; test_normalise_proto_key_unicode_brackets); tightened row-count minimums (papakupu ≥ 4,600; source_abbreviations ≥ 145); paekupu content_hash backfilled via --refresh (16,486 rows); 102 tests all pass | ✓ Done | — |

| 30 | Source abbreviations — `source_abbreviations` table added to schema; 97 rows (90 Te Aka from official bibliography + 7 Williams inline); `expand_citations()` utility in utils.py; `scripts/30_source_abbrevs_import.py` | ✓ Done | — |
| 31 | Refresh tracking — `content_hash` + `first_seen` columns on te_aka_entries and paekupu_entries; `data_refresh_runs` + `data_refresh_log` tables; `compute_content_hash()` in utils.py; `--refresh` flag on both import scripts for incremental diff with per-field change logging; backfill step handles NULL hashes from pre-tracking imports | ✓ Done | — |
| 32 | Source abbreviation coverage for all five dictionaries — 47 He Pātaka Kupu abbreviations added to `source_abbreviations` (source_dict='hepatakakupu'); `05_hepataka_import.py` updated to merge te_aka + hepatakakupu abbrevs and expand usage_examples at import time; 24,941 HPK entries re-imported with expanded citations. POLLEX: already stores source_author (full expansion) in DB — no changes needed. Papakupu: source_code values are contributor/informant codes (WAI, NKM, etc.) with no PDF key — not expandable. Paekupu: no inline citation patterns in text — confirmed no action needed. Table now has 145 total rows. | ✓ Done | 30 |

| 33 | `pollex_languages` reference table — schema (language_slug PK, language, iso_code, subgroup, region, country_or_island_group, notes); seed all 67 POLLEX languages with ISO 639-3 codes and subgroup classification (Tongic / Eastern Polynesian / Samoic-Outlier / Polynesian Outlier / Fijian / Rotuman / Other Oceanic); index on pollex_reflexes.language_slug linking to new table; 6 new tests (schema, seed completeness, subgroup coverage, reflex join, known entry, Tongic join); 108 tests all pass | ✓ Done | — |
| 34 | Update DATABASE_REFERENCE.md — `pollex_languages` table schema added (67 rows, 7 subgroups, ISO codes); cross-language reflex queries (filter by subgroup/region); `cross_source_candidates` + `cross_source_detection_runs` tables added (181,938 pairs); cross-source "also in" query example; "What the App Must Handle" item 8 added | ✓ Done | 33 |
| 35 | Cross-source duplicate detection — `cross_source_candidates` + `cross_source_detection_runs` tables; `scripts/35_detect_pairs.py` (181,938 pairs across 15,543 shared headwords: HPKxTeAka=58,318; HPKxWilliams=31,003; TeAkaxWilliams=22,141; HPKxPaekupu=14,508; etc.); `scripts/35_review_candidates.py` human CLI (a/d/s/q); Claude Code reviews unreviewed batches and writes ai_reasoning + status; 7 new tests; 115 tests all pass | ✓ Done | — |
| 36 | Papakupu definition cleanup — stripped the extracted leading `{source_code}` and `[part_of_speech]` tokens from `papakupu_entries.definition` (`scripts/09_papakupu_clean_definitions.py`, dry-run default + `--apply` + auto-backup); 3,774 / 4,683 rows cleaned (1,658 source braces + 2,574 POS brackets removed). Only the first/extracted occurrence removed (per-sense duplicates kept); uppercase example codes `[TTU]`/`[NGH3]`, `[041126]`, sense `[1]` retained; 688 page-code `source_code`s (e.g. `117/T`) were never braces so untouched; 9 stub defs (def was only the code) now empty (ids 32,97,139,207,303,971,1020,1195,1251). FTS auto-synced via trigger; integrity_check ok; backup `data/maori_dict.db.bak-20260618-223719`. DATABASE_REFERENCE.md updated. Also fixed 3 pre-existing test failures: (a) `pollex_cognatesets` `source_metadata.entry_count` 2931→3291 (360 `origin='ancestor_only'` rows were uncounted; `pollex_ancestry.py` now syncs the count); (b/c) `test_papakupu_parse` `test_page_range` + `test_sense_numbers_reasonable` now skip None `pdf_page`/`sense_number` (session-6b website entries legitimately lack them). 164 tests all pass. | ✓ Done | 6 |

| 37 | DATABASE_REFERENCE.md accuracy audit — verified every row-count, etymology, and reachability claim against `data/maori_dict.db`. All table counts correct (etymology_links 229 = 184 ACD + 57 LPO, 12 both; protoform_ancestry 1,599 = 706 pollex + 485 acd + 408 lpo; match-method split 169/36/13/11; reachability 1,189 of 2,931 reach higher level, 325 reach ACD/PAN). Fixed 2 stale items: DB size `~78 MB`→`~111 MB`; etymology coverage line clarified to "7.8% of Māori-relevant POLLEX cognatesets (229 / 2,931)" (denominator was ambiguous vs 3,291 total). | ✓ Done | 34 |

| 38 | Papakupu `[n.]` part-of-speech fix — 21 `papakupu_entries` rows had a noun marker `[n.]` embedded in `definition` (all with `part_of_speech` NULL). Stripped the ` [n.]` token (case-insensitive, whitespace collapsed) and set `part_of_speech='Noun'` (matches existing 871 "Noun" rows). Other bracket tokens left untouched (uppercase source/citation codes `[TTU]`/`[NGH3]`, domain tags `[tennis]`/`[computer]`, sense `[1]`). FTS auto-synced via `papakupu_fts_upd` trigger; verified 0 `[n.]` remaining. Backup `data/maori_dict.db.bak-20260621-140814-nfix`. **Follow-up scan** for other inline POS abbreviations → 123 more NULL-POS rows fixed: lowercase whole-entry bracket markers (`[v.]` `[v.t.]` `[v.i.]` `[adj.]` `[adv.]` `[noun]` `[verb]` `[pron]`) + bare leading abbreviations across the Tai Tokerau source (`n.`/`v.`/`u.`/`adj.`/`adv.`/`v.t.`/`v.i.` prefixes, e.g. `v.t. to slice…`, `u. count…`, `n. door…`). Mapping: n/noun→Noun, v/verb→Verb, v.t.→Verb (transitive) (20), v.i.→Verb (intransitive) (8), adj.→Stative, adv.→Universal, pron→Pronoun, multi-abbrev leads (`adj., v.i.` etc)→Universal; totals Universal 51, Noun 22, Verb(t) 20, Verb 13, Stative 9, Verb(i) 8. **Deliberately left untouched:** 168 Capitalized inline sense labels (`[Noun]`/`[Verb]`/`[Adverb]`) inside multi-sense narrative defs (16 carry >1 distinct label) — they mark per-sense POS and flattening is destructive; plus 2 inline-in-narrative rows (2704, 2740). FTS auto-synced; backup `data/maori_dict.db.bak-20260621-141849-posfix`. | ✓ Done | 36 |

| 39 | Papakupu inline variant-block strip — `scripts/10_papakupu_strip_variant_blocks.py` (dry-run default + `--apply` + auto-backup). `02_papakupu_extract.py` parsed the leading `<...>` variant list into `variant_forms` but never removed it from `definition`, leaving e.g. `< koohure, kohure >` duplicated. Removed the FIRST `<...>` block per row: 557 rows where the comma-split contents exactly equalled `variant_forms` (delete only); 1 row (id 3996 `ahi kā`) had empty `variant_forms` so the 3 forms were added + keys recomputed; 3 OCR-malformed blocks closing with `]` not `>` handled explicitly (id 1062 `kōmata`, 3388 `tūpāpaku`, 3399 `tupuna`) — variants added + stripped. 561 defs changed, 0 emptied. **Deliberately left untouched:** ~16 run-on sub-entries whose `<...>` belongs to a DIFFERENT headword (e.g. id 19 `ahi` contains `ahi kā`'s block) and junk `<#>` markers — separate run-on-extraction problem, not variants of the row's own headword; plus stray `<Eng.`/`< nurse`/`< ara [2]` etymology & see-also arrows. `variant_search_keys` recomputed for add cases via `normalise_search_key` (matches import logic). FTS auto-synced via `papakupu_fts_upd` trigger; integrity_check ok; backup `data/maori_dict.db.bak-20260621-143139-varstrip`. | ✓ Done | 36 |

| 40 | Canonical schema design (for app build) — `SCHEMA_PROPOSAL.md`: researched OntoLex-Lemon / TEI Lex-0 / Wiktextract-Kaikki; analysed all 5 word-list source schemas + POLLEX. Found two lossy/ambiguous columns: `definition` (English in Williams/Te Aka/Papakupu, **Māori** in He Pātaka Kupu, both in Paekupu) and `usage_examples` (flat string[], 4 shapes; **Papakupu drops the Māori sentence**, storing only English+`[SRC]` — same `02_papakupu_extract.py` root cause as the run-on bug). Proposed unified app-serving SQLite core: `entry → form → sense → example` + `relation` + `entry_domain`, keeping one row per source (no merged headword; link via existing `cross_source_candidates`), `gloss_en`+`gloss_mi` split, structured `example(text_mi,text_en,source_abbrev,citation)`. Doc written for another LLM to assess migration impact on the existing app (§7 old→new field map, breaking-changes checklist, backward-compat views). §8 example attribution (papakupu 99.9% / te_aka 51% / hepataka 42%). §9 POLLEX = etymology layer, not a 6th dictionary (link entry↔cognateset). Built review artifacts in `data/maori_dict.db` (additive, source tables untouched): `scripts/12_build_standard_sample.py` → `std_entry/std_sense/std_example/std_form/std_relation/std_entry_domain` (10 entries/source) + `std_review` view; `scripts/13_build_pos_normalisation.py` → `std_pos` (315 distinct raw POS, per-source counts, `canonical_en`+`canonical_mi`+`status`; 66 seeded = 73.7% of entry-rows, Māori from Paekupu pos_mi; 249 needs_review, mostly HPK compound codes `mahp, ing, āhua`). **NOT YET APPLIED to prod schema** — proposal + samples only. | ✓ Done | 34, 35 |
| 41 | Papakupu POS-fix reconstruction (precursor to extractor re-import) — `scripts/11_papakupu_pos_fix.py` reconstructs the ad-hoc session-38 inline-POS extraction (bracket tokens `[n.]`/`[v.t.]`/… + bare leading abbrevs `u.`/`n.`/`v.t.`; NULL-pos rows only) as a re-runnable script, derived by diffing the session-38 backups (`bak-140814-nfix` → `bak-141849-posfix` → `bak-143139-varstrip`). **Validation closed** — full nfix→varstrip corpus diff (correct ground truth is `varstrip`, not the mid-fix `posfix` snapshot): reconstruction sets POS on 145 rows with **0 misses, 0 POS-value disagreements**; the 19 remaining diffs are **whitespace-only** (script strips ends but does not reflow internal `\n` runs — session-38's collapse was an incidental editor artifact, deliberately not reproduced). New `tests/test_papakupu_pos_fix.py` (20 tests): unit cases for both marker styles + exclusions (`pron`, domain tags, skip-prefixes) + the documented whitespace behaviour, plus an optional corpus guard that re-runs the nfix→varstrip diff when the (gitignored) backups are present and asserts every divergence is whitespace-only. 250 tests all pass. Script unchanged — validated as-is. Still the precursor to the deferred upstream extractor fix (`02_papakupu_extract.py` run-on splitting + Māori example extraction → `example.text_mi`). | ✓ Done | 38 |

| 42 | Canonical unified core **implemented** (SCHEMA_PROPOSAL.md §3, post-Tikinari-review). `00_init_db.py`: added `entry/form/sense/example/relation/entry_domain` + `entry_fts/sense_fts/example_fts` (external-content, trigger-synced) + `source_metadata.default_dialect` (Papakupu→'Tai Tokerau'). `scripts/50_build_unified.py` — idempotent per-source projection (`--source <name>|all`) from the curated `*_entries` tables: entry 105,898 / sense 105,898 / example 90,019 / form 12,955 / relation 103,485 / entry_domain 59,570. Language-tagged (HPK→`gloss_mi` only, Te Aka→`gloss_en`, Paekupu bilingual 3,433); Te Aka synonyms resolved to `target_entry_id` (87,442/87,506); FK-clean. Decisions: this-repo-only (Tikinari `build_app_db.py` rewrite deferred); bare table names; POS raw passthrough (canonical `std_pos` wiring after expert review); grain = 1 source row→1 entry→1 sense (multi-sense grouping deferred); Papakupu `example.text_mi` NULL pending the upstream extractor fix. **Bug fixed:** HPK `word_id` not unique per row → `source_entry_id` now uses HPK row PK (was 1,181 device-id collisions). **Perf fix:** added FK indexes `idx_sense_parent`, `idx_relation_target`, `idx_entry_domain_sense` (per-source DELETE was full-scanning on FK checks). `tests/test_unified.py` (18 tests). Docs: `docs/UPDATE_WORKFLOW.md` (per-source runbook) + `.claude/skills/update-dictionary/` skill; DATABASE_REFERENCE.md gained the unified-core section. **PDF demo deferred** (decide source later). DB VACUUMed. | ✓ Done | 40 |

| 43 | Papakupu full re-extract + repopulate (PDF + website) — re-ran the pipeline end-to-end: `02_papakupu_extract.py` (PDF→JSON, 3,994) → `02_papakupu_website_update.py` (merge 9 dict gaps + 680 deduped WRRT-TAPEHA terms → `papakupu_entries.json` 4,683, re-import to `papakupu_entries`) → in-DB cleanups `09`/`10`/`11 --apply` (defs cleaned 3,774; variant blocks stripped 561; NULL-POS set 148) → `50_build_unified.py --source papakupu` (entry 4,683 / sense 4,683 / example 9,693 / form 1,077 / relation 1,163; dialect=Tai Tokerau). Counts identical to prior state (idempotent reproduction). Deleted stale `sources/papakupu/website/gap_report.json` (447 "website_only" were all already present — 431 exact + 16 case/macron variants). Note: `website_update` is not idempotent (WRRT rows append unconditionally) so it must run on a fresh PDF-only JSON — order enforced. `example.text_mi` still NULL (known upstream extractor bug). Backup `data/maori_dict.db.bak-20260627-083353-papakupu`. 182 tests all pass. | ✓ Done | 42 |

| 44 | **Database split into two files** (so the bloated working DB no longer travels to the app repo). Renamed the full working DB `data/maori_dict.db` → **`data/staging_dictionary.db`** (via `VACUUM INTO`; 81 tables, ~210 MB) and dropped the session-40 prototype/test tables `std_entry/std_sense/std_example/std_form/std_relation/std_entry_domain` + the `std_review` view (kept `std_pos`). New **`scripts/60_export_app_db.py`** builds a slim **`data/maori_dict.db`** (~114 MB) as a schema-driven projection of the app surface only: unified core (`entry/sense/example/form/relation/entry_domain` +FTS) + etymology layer (`pollex_cognatesets/pollex_reflexes/pollex_languages/lpo_cognatesets/acd_cognatesets/etymology_links/protoform_ancestry/reconstruction_levels`) + `source_abbreviations/source_metadata`. Verified: no staging tables leak into the app DB, integrity_check ok, foreign_key_check clean, core joins + FTS + etymology joins functional. Retargeted all 15 build/import scripts + `utils.DB_PATH` + `tests/test_schema.py` to write `staging_dictionary.db`; `60_export` is the new final pipeline step. Docs updated: `DATABASE_REFERENCE.md` (two-DB model + specs), `docs/UPDATE_WORKFLOW.md`, update-dictionary skill, `SCHEMA_PROPOSAL.md`. Pre-split backup `data/maori_dict.db.bak-presplit-20260627`. | ✓ Done | 42, 43 |

| 45 | Williams multi-sense split + sense-level POS + entry POS wrap-up (all sources) — `scripts/williams_senses.py`: parser splits numbered Williams senses (i, ii, iii…) into separate rows, each with `sense.part_of_speech` (canonical English label from inline abbreviation); `scripts/14_seed_williams_pos.py`: seeds `std_pos` with Williams abbreviations (66 rows; Māori labels from Paekupu `pos_mi` where matched); `scripts/50_build_unified.py` + `scripts/00_init_db.py` updated: `sense.part_of_speech` added to schema; `entry.part_of_speech_en` / `entry.part_of_speech_mi` computed as deduped, comma-joined wrap-up of sense POS via `std_pos` (mi only where `std_pos.canonical_mi` set; HPK wrap-up NULL — HPK codes not yet in `std_pos`). Williams: 11,910 entries → 20,193 senses. Total senses across all sources: 114,181. App DB rebuilt (`scripts/60_export_app_db.py`): 116.9 MB, integrity_check ok, foreign_key_check clean. New files: `scripts/williams_senses.py`, `scripts/14_seed_williams_pos.py`; modified: `scripts/00_init_db.py`, `scripts/50_build_unified.py`; new tests: `tests/test_williams_senses.py`, `tests/test_williams_pos_seed.py`; updated: `tests/test_schema.py`, `tests/test_unified.py`. 191 tests all pass. | ✓ Done | 44 |

| 46 | Te Aka multi-sense split + per-sense POS + per-sense examples — **parser bug fix.** `04_te_aka_parse.py` already extracted per-sense POS (from `<strong>(verb)</strong>` etc.) but the aggregation step discarded all but the first sense's POS and packed every sense into one `definition` string (`"1. … \| 2. …"`). Rewrote `parse_page` to emit a structured `senses[]` array (per-sense `sense_number`/`part_of_speech`/`gloss_en`/`definition_raw`/`examples`/`citations`/`synonyms`); `gloss_en` strips the leading passive marker `(-ia,-ngia)` (kept in `definition_raw`) — same convention as Williams. Legacy `definition`/`usage_examples` kept for FTS + content-hash. `04_te_aka_import.py`: new `senses` JSON column wired through DDL/insert/material-hash/refresh-diff. `50_build_unified.py` `build_te_aka`: explodes `senses[]` into per-sense `sense` rows (own POS) with per-sense `example` rows; exact-duplicate senses collapsed + survivors renumbered 1..n (e.g. `Rua o Takurua, Te` word_id 40668: 11 identical → 1); fallback to packed def for pre-change data. Re-parsed 47,955 raw HTML (47,878 entries) → re-imported → re-unified → re-exported app DB. **Te Aka: 47,878 entries → 59,472 senses (avg 1.24)**, per-sense POS recovered (e.g. `heke` senses 1–6 verb / 7–10 noun); 177 dup senses collapsed; 1,911 senses (3.2%) NULL POS where source omits it. Total senses across all sources: 125,775. App DB `data/maori_dict.db` rebuilt 121.5 MB, integrity_check ok, FK clean. Modified: `scripts/04_te_aka_parse.py`, `scripts/04_te_aka_import.py`, `scripts/50_build_unified.py`. Docs updated: `DATABASE_REFERENCE.md`. 26 unified tests pass. | ✓ Done | 45 |

| 47 | Williams `‖` cross-reference extraction — **parser bug fix.** Williams' double-bar `‖` ("compare") marker and its target were leaking into `definition` → `gloss_en` (reported: apū sense 2 gloss carried `‖ apa (i), 2.`). Affected 3,859 / 11,910 entries (32%); the old `cross_refs` capture (bold foreign-mi spans) was ~73% noise and caught only 115. New `scripts/williams_xref.py` `extract_xrefs(definition) -> (clean_def, [{type,target}])`: strips each `‖`-span, classifies target as `see_also` (Māori headword ref, e.g. `apa (i), 2`) or `citation` (literature ref, e.g. `J. vii, 120`, `Wai 23`). Handles mid-entry `‖` before a later numbered sense (`Hapū`), ref-internal sense pointers (`, 2.` not read as a new sense), citations with internal periods/multi-source runs, multiple `‖` per entry; leaves `‖` inline when it sits in parentheses (`Manawa`) or is followed by a swallowed example sentence / editorial note (dangling-paren or note-word guard — 65 entries left inline, no data loss). Pure-pointer stub entries (756) whose whole gloss was a `‖` ref get a synthesised `Cf. <targets>.` gloss (no empty defs). `01_williams_parse.py`: imports the extractor, removed the noisy bold-span capture, cleans `definition` + populates typed `cross_refs`, keeps stubs. `50_build_unified.py`: `cross_refs` dicts → `relation(rel_type=see_also|citation)`. Re-ran parse → import → unified: **3,903 see_also + 213 citation** relations (was 115 noisy `cross_ref`). New: `scripts/williams_xref.py`, `tests/test_williams_xref.py` (17 tests); modified: `scripts/01_williams_parse.py`, `scripts/50_build_unified.py`. `cross_refs` JSON shape changed (string[] → {type,target}[]). 64 williams+unified tests pass. **App DB not yet re-exported** (`60_export_app_db.py`). | ✓ Done | 45, 46 |

| 49 | Te Aka synonym `target_entry_id` — recover the last 64 NULLs (0.07% of 87,506). **Root cause:** synonyms resolve by `word_id` (== `entry.source_entry_id`), but 10 referenced word_ids were absent from the corpus: 4 entries never imported (failed scrape — `connection_error`/`http_500`/`http_503`, NOT 404) and 6 referenced stale word_ids Te Aka has since renumbered (e.g. synonym word_id=8095 → `tumera`, now under source_entry_id 37049). **Two fixes:** (1) `scripts/50_build_unified.py` `resolve_te_aka_synonyms` gained a **headword-fallback pass 2** — for rows still NULL after word_id match, link by `headword_search` when exactly one te_aka entry carries that key (homographs skipped to avoid mislinking); returns recovered count. (2) **Re-scraped 10 missing word_ids** (8094 tīmatatanga, 8095 tumera, 8096 Timi, 8097 timo, 12008 pūrangiaho, 14429 pukapuka raiheni, 14430 pukapuka rēhita, 14666 Tieki, 41137+41138 heuheu ×2 homographs) — all live (200, valid `word-def`), downloaded to `sources/te_aka/raw/`, manifest errors 40→30, valid 47,955→47,965. Re-ran parse (47,888 entries, +10) → import --refresh (10 new, 0 modified) → `50_build_unified.py --source te_aka` → `60_export_app_db.py`. **Result: te_aka synonym NULL target_entry_id 64 → 0** (all 87,569 relations resolved); pass-1 word_id match now exact for all (better precision than headword guessing). App DB `data/maori_dict.db` 121.6 MB, integrity_check ok, FK clean. Modified: `scripts/50_build_unified.py`. | ✓ Done | 48 |

| 48 | Williams `see_also` → `target_entry_id` resolution (clickable links). The session-47 `‖` extraction left all 3,903 `see_also` (and 213 `citation`) `relation` rows with `target_entry_id` NULL. New `parse_see_also_targets(raw)` in `scripts/williams_xref.py` strips Williams' printed decoration into linkable `(search_key, roman_sense)` tuples — handles bare headwords (`pakuhā`), `(i)`/`(ii)` sense pointers (`hea (i)`), trailing sense-of-X number pointers (`apa (i), 2` → just `apa (i)`), and multi-target comma lists (`mataaho, tiaho`); leaves cognate/relative/citation pointers unresolved (leading uppercase abbrev `Tah, ao`/`F`, relative `6, below`). `resolve_williams_xrefs(con)` in `50_build_unified.py` (mirrors `resolve_te_aka_synonyms`, runs in `unify_source` after `build_williams`): builds a `headword_search → [(entry_id, roman_sense, headword)]` map over Williams entries (joined `entry.source_entry_id` → `williams_entries`), matches each target preferring the homograph with the same roman sense (else any same-headword entry, same-source), and **splits** multi-target rows — first match updates the row, each further match inserts a new `see_also` row (raw kept in `note`). **Result: 3,490 / 4,230 see_also rows resolved (83%)** (split inserts grew the 3,903 originals to 4,230); citations untouched. Re-ran `50_build_unified.py --source williams` + `60_export_app_db.py` (121.5 MB, integrity_check ok, FK clean). New: `parse_see_also_targets` + 11 parser tests + 5 DB-resolver tests in `tests/test_williams_xref.py` (33 williams_xref tests; 59 with unified). Modified: `scripts/williams_xref.py`, `scripts/50_build_unified.py`. Docs: `DATABASE_REFERENCE.md` relation row updated. | ✓ Done | 47 |

| 50 | Papakupu bilingual example extraction — **the long-deferred upstream extractor fix** (text_mi was NULL since session 5). `02_papakupu_extract.py` `extract_examples()` now returns `{text_mi, text_en, source_abbrev}` dicts instead of English-only strings. Papakupu examples are `<Māori>. <English>. [SRC]`; old `EXAMPLE_RE` captured only the sentence before `[SRC]` (the English translation), dropping the Māori half. New approach: split each segment on the `[SRC]` ref (`SRC_REF_RE`), take the last sentence as `text_en` and the one before it as `text_mi`. The **first** example per entry has its Māori glued onto the English gloss prose with no separating period, so new `_maori_tail()` + `_is_maori_token()` recover it by walking the sentence right-to-left and stopping at the first clearly-English token (Māori alphabet = vowels±macrons + h k m n p r t w; English-only consonants b c d f j l q s v x y z mark the boundary). `50_build_unified.py` `build_papakupu` rewired to read the dict shape and write real `text_mi`/`text_en`/`source_abbrev`. Re-ran full pipeline (extract→website_update→09/10/11 --apply→50→60). **Result: papakupu `example.text_mi` 0 → 10,071 of 10,211 (98.6%)**; ~140 unrecovered are bracketed literal-notes / single-sentence edges (expected residue). Example count 9,693→10,211 (new `[SRC]`-segmentation captures more). New `tests/test_papakupu_examples.py` (10 tests); modified `scripts/02_papakupu_extract.py`, `scripts/50_build_unified.py`. Docs updated: `DATABASE_REFERENCE.md`, `docs/UPDATE_WORKFLOW.md`. Both DBs rebuilt (staging + app `maori_dict.db` 122.3 MB); backups `data/staging_dictionary.db.bak-20260628-1854*`. 260 tests all pass. (SKILL.md note edit blocked by config-write guard — still says text_mi NULL; update manually.) | ✓ Done | 42, 43 |

**HPK entry POS wrap-up — verified DONE (no session needed).** The s45 caveat ("HPK wrap-up NULL — HPK codes not yet in `std_pos`") is stale. HPK atomic codes (`ing`/`mahp`/`mahw`/`āhua`/`tūkē`/`ptm`…) are in `std_pos`; `50_build_unified.py` explodes the comma-joined `sense.part_of_speech` (`"mahp, ing, āhua"`) into atomics, maps each, dedupes. Coverage: `entry.part_of_speech_en` 24,829/24,941 (99.6%), `part_of_speech_mi` 24,940/24,941. Residue left as-is by decision: 112 en-NULL entries driven by 2 atomic codes seeded with Māori only (`thu`→Tūhau, `tkp`→Tūkapi — `canonical_en` deliberately left NULL); 1 mi-NULL entry `heoi` (sole sense POS = `unsure`, unclassifiable).

| 51 | Etymology linking — missed-opportunity sweep across ACD/LPO/POLLEX. **(1) New reverse bridge `pollex_entry_links`** (`scripts/08b_pollex_entry_linker.py`): joins each POLLEX **Māori reflex** → unified `entry` by macron-neutral headword (`normalise_search_key`), so the app can surface the whole proto-tree on a word the user looked up. **48,243 links → 20,779 distinct entries** across all five dictionaries (hepatakakupu 21,114 / te_aka 12,245 / williams 7,573 / papakupu 4,529 / paekupu 2,782); 2,825 / 3,291 cognatesets covered. New table in `00_init_db.py` (cognateset_id, reflex_id, entry_id, match_key, match_method, match_confidence) + 2 indexes; added to `60_export_app_db.py` `APP_TABLES`. **(2) `08_etymology_linker.py` recall fixes** → etymology_links **229 → 421** (+192): `LEVEL_MAP` gained the Polynesian levels PN→PPn, NP→PNPn, CE→PCEPn (LPO's own Polynesian reconstructions, previously never attempted); match keys now macron-folded (`_norm` = `normalise_sort_key∘normalise_proto_key`, no double-vowel collapse) and comma-joined multiform `name_key`s split + indexed per part; slash-levels (`PAn/PMP`, `PCP/PPn`) filed under each component; new **Tier 3** `tier3_bare_citation` parses bare `POC */PMP */PAN *` refs in notes (no quoted gloss / no `(ACD)`/`(LPO)` tag), gloss-validated against the set's own description (conf 0.65). **Ruled out** (checked, low yield): direct ACD PWMP/PPH mapping (~3 form overlaps — Polynesian vs Philippine form shapes; deep ACD already reached via `protoform_ancestry`). Both linkers idempotent with `--reset`; app DB re-exported (`maori_dict.db` 125.5 MB, integrity_check ok, FK clean). New: `scripts/08b_pollex_entry_linker.py`; modified: `scripts/00_init_db.py`, `scripts/08_etymology_linker.py`, `scripts/60_export_app_db.py`. Tests: `pollex_entry_links` added to schema checks + new `TestPollexEntryLinks` (5) + `test_etymology_links_polynesian_level_matches`; 122 full-suite + 26 schema/unified all pass. Docs: `DATABASE_REFERENCE.md` (new table + 421 count + Tier-3 method), `docs/UPDATE_WORKFLOW.md` (08b step, after `50_build_unified.py`). | ✓ Done | 42, 44, 50 |

**Etymology unification (`ETY_*`) — planned, sessions 52–59.** Plan file: `C:\Users\richardk\.claude\plans\etymology-unification-ety.md`. Give etymology the same stage→unify→export pipeline as the word-lists; add Tregear (NZETC TEI, public domain) + ABVD (`lexibank/abvd`, CC-BY-4.0) as comparative sources; Walworth (`lexibank/walworthpolynesian`, CC-BY-4.0) as gap-fill only. App DB hard-cutover to unified `ETY_*` tables (no compat views — new app-DB format not yet shipped).

| # | Session | Status | Depends On |
|---|---------|--------|------------|
| 52 | Tregear scrape — `scripts/17_tregear_scrape.py` → `sources/tregear/raw/`. **Plan URL was stale** (verified live per the "check URLs first" rule): `nzetc.victoria.ac.nz` now 302-redirects to the **National Library web-archive** (`ndhadeliver.natlib.govt.nz`), whose viewer is a WABAC service-worker frameset (empty body). Fix: fetch raw archived HTML via the pywb **`id_` identity modifier** (`…/webarchive/20210104000423id_/http://nzetc…/tei-TreMaor-*.html`). **Second plan correction:** the dictionary body is `tei-TreMaor-**c1-1..c1-15**` (the 15 Māori letter sections A E H I K M N NG O P R T U W WH), **not** `b3-*` — `b3-1..b3-25` is the English→Māori index (A–Z incl. B/C/D/F…, non-Māori letters). Scraper reads the TOC, harvests `c1-*` links dynamically, also grabs `f8` (ABBREVIATIONS) + `f7-14` (geo/dialect refs). Resumable, `--limit`/`--force`, 3s delay, manifest.json. **18 pages, 0 errors** (15 letters + TOC + 2 reference). Licence = **CC BY-SA 3.0 NZ** (plan said "public domain" — the 1891 text is PD by age but NZETC's digitisation is CC-BY-SA). | ✓ Done | — |
| 53 | Tregear parse + import → staging `tregear_entries` + `tregear_cognates`. `scripts/17_tregear_parse.py` (HTML→`sources/tregear/tregear_parsed.json`) + `17_tregear_import.py` (JSON→staging). **Entry** = `<p><b>HEADWORD (qualifier)</b> (<i>pron</i>), gloss. Cf… 2. sense</p>`; homonyms repeat (4× "HA"), derived forms are their own entries (`Whaka-HA`; 523 `Whaka-` headwords). **Cognate** = `<p class="pad-left"><b>Language</b>&mdash;form, gloss</p>` — dictionary body keys cognates by **full language name** (Samoan/Hawaiian/Tongan…), **not** the `Sam./Tah.` abbrevs the plan assumed; `Ext. Poly.:` prefix → `extra_polynesian=1`. Parser handles: in-`<b>` qualifiers `(myth.)`/`(passive)`/`(or var):`, en/em/ascii dashes, the occasional un-bolded first cognate (`Samoan-fa…`), continuation-paragraph append, page-break `tei_ref`; long vowels are grave/circumflex-marked (`hà`), not macrons. Language OCR variants folded (Paumoutan→Paumotan, Mangrevan→Mangarevan, Fiji→Fijian…); `POLYNESIAN` set drives `extra_polynesian`. `headword_norm` = macron/vowel-folded, space/hyphen-stripped key (`Whaka-HA`→`whakaha`) for the S57 entry bridge. **Result: 9,638 entries (8,152 distinct headwords, 1,096 homonym groups), 12,563 cognates (200 languages; 2,277 extra-Polynesian); 0 orphan FKs, 0 null norms.** `source_metadata.entry_count=9638`. Tables in `00_init_db.py`; new `tests/test_tregear.py` (7). Staging-only (no unify/app export until `ETY_*` S57/S59); suite kept green by adding `tregear` to `test_full_suite.EXPECTED_SOURCES`. **274 tests pass.** | ✓ Done | 52 |
| 54 | ABVD import — cloned CLDF `lexibank/abvd` (CC-BY-4.0) → `sources/abvd/`. New staging tables `abvd_parameters`/`abvd_languages`/`abvd_cognatesets`/`abvd_forms`/`abvd_cognates` (declared in `00_init_db.py`); importer `scripts/07b_abvd_import.py`. ABVD has no protoforms — a cognateset is an attested cognacy class per concept (e.g. `hand-1`). Polynesian membership resolved by matching ABVD langs against curated `pollex_languages` (ISO 639-3 + name): **144 Polynesian/Oceanic lects** (Eastern Polynesian incl. Māori `mri`, Fijian, Tongic, Samoic-Outlier, Outlier, Rotuman). Filter = keep cognatesets with ≥1 Polynesian member → **3,020 / 19,356 cognatesets, 158,378 forms, 181,023 cognate judgments, 1,946 langs**. FK-clean (0 orphans), 0 sets with 0 poly members; `source_metadata.entry_count=3020`. Spot-check: Māori `ringa` ↔ Fijian `liga` in `hand-34`. Tests deferred to S59. | ✓ Done | — |
| 55 | Walworth import — cloned CLDF `lexibank/walworthpolynesian` (CC-BY-4.0) → `sources/walworth/`. New staging tables `walworth_parameters`/`walworth_languages`/`walworth_cognatesets`/`walworth_forms`/`walworth_cognates` (declared in `00_init_db.py`); importer `scripts/07c_walworth_import.py` mirrors `07b_abvd_import.py`. A Polynesian-only LingPy cognacy wordlist (Walworth 2014) — cognateset IDs are bare integers (the form's `Cognacy` value), expert-coded per concept. Imported **full + unfiltered** (no Polynesian-member filter; whole set is already Polynesian): **1,456 cognatesets, 7,518 forms, 7,518 cognate judgments (all `doubt=false`, `method=expert`; 213 loans), 210 concepts, 31 languages**. Languages classified against curated `pollex_languages` for subgroup tagging only (no drop): 28 matched (Eastern Polynesian 12 / Polynesian Outlier 7 / Samoic-Outlier 6 / Tongic 3); 3 unmatched = `Fesau Futuna`, `Nukeria`, and **`Proto Polynesian`** (a reconstructed pseudo-language — carries PPn protoforms, useful for S58 gap-fill). FK-clean (0 orphan forms/cognates/sets, 0 NULL-parameter sets). Spot-check: Māori `ringa` ↔ concept `hand`. `source_metadata.entry_count=1456`. Staging-only (no unify/app export — `ETY_*` cutover is S59); Walworth-specific tests deferred to S59, but suite kept green by adding `abvd`+`walworth` to `test_full_suite.TestSourceMetadata.EXPECTED_SOURCES` (was failing for ABVD too since S54). 267 tests all pass. | ✓ Done | — |
| 56 | `ETY_*` schema + POLLEX/LPO/ACD projection. Six new tables in `00_init_db.py` (`ETY_level`/`ETY_language`/`ETY_cognateset`/`ETY_reflex`/`ETY_link`/`ETY_entry_link`) with provenance cols on set/reflex (`source`, `source_ref` raw-id traceback, `gap_fill`) + FK indexes (incl. the plan-named `idx_ety_reflex_set`/`idx_ety_link_target`/`idx_ety_entry_link_entry`). New `scripts/52_build_etymology_unified.py` — pure DB→DB projection mirroring `50_build_unified.py`: idempotent, per-source `--source pollex\|lpo\|acd\|all`, `--reset` full wipe. Refs (`ETY_level`←`reconstruction_levels` 25, `ETY_language`←`pollex_languages` 67) rebuilt every run. **Cognatesets: 16,968** (pollex 3,291 + lpo 2,820 + acd 10,857). **Reflexes: 49,760** (POLLEX only — LPO/ACD carry no per-language reflexes in staging). `proto_key`=`normalise_proto_key(protoform)` on every set for the S57 cross-source dedup (2 legit empties: bare-infix ACD forms `<in>`/`<um>`); `set_group` stashes ACD `etymon_id` / LPO `chapter_id` for S57 ancestry. Built-in **parity proof** (non-zero exit on mismatch): per-source raw-vs-ETY counts + sampled per-set reflex join — **PARITY: PASS**, 0 orphan reflexes, `foreign_key_check` clean. `ETY_link`/`ETY_entry_link` created-but-empty (populated S57). New `tests/test_etymology_unified.py` (8) — 137 tests pass. | ✓ Done | 18, 19, 20 |
| 57 | Fold Tregear + ABVD into the ETY build; cross-source dedup; `ETY_link` + `ETY_entry_link` populated. `scripts/52_build_etymology_unified.py` extended (`SOURCES` now pollex/lpo/acd/**tregear/abvd**). **Cognatesets 16,968 → 29,626** (+tregear 9,638 [1 set/headword] +abvd 3,020). **Reflexes 49,760 → 252,984** (+tregear 22,201 = 9,638 synthetic Māori headword reflexes + 12,563 per-language cognates; +abvd 181,023 = every cognacy judgment). **Design:** Tregear is comparative-by-headword → each entry = 1 `ETY_cognateset` (protoform = headword, `proto_key`=`normalise_proto_key`), reflexes = the Māori headword itself (`lang_key='maori'`) + its cognates; ABVD = attested per-concept class → `proto_key` **NULL by design** (no reconstructed form, excluded from dedup), reflexes = judgments joined form→lang. `ETY_language` 67 → 2,060 (+tregear 186 +abvd 1,807; non-POLLEX langs resolved onto POLLEX slugs by ISO/name where possible — Māori mri→`maori` — else prefixed `abvd:`/`tregear:` keys). **`ETY_link` 2,203:** merged `etymology_links` (530 equivalence, split per lpo/acd target) + `protoform_ancestry` (1,358 ancestry; the 241 rows already `source='etymology_links'` skipped to avoid double) + **cross-source dedup 315** (proto_key match + gloss-Jaccard ≥0.20 reusing 08_etymology_linker thresholds; all lpo↔acd since pollex proto_key is level-prefixed and pollex↔lpo/acd is already carried by the etymology_links merge). 0 orphan raw-ids; 0 self/orphan links. **`ETY_entry_link` 114,620 → 41,542 entries** (tregear 61,789 / pollex 48,243 / abvd 4,588); extends 08b logic to **every source's Māori reflexes** (macron-neutral `normalise_search_key` bridge to unified `entry`) — pollex slice **48,243 = exact match** to standalone `pollex_entry_links` (regression guard). Decision: `ETY_reflex` keeps `entry_id` only in `ETY_entry_link` (not denormalised inline). **Perf/idempotency fix:** clear `ETY_link` + `ETY_entry_link` before the per-source delete loop — with `foreign_keys=ON`, deleting 181k abvd reflexes referenced by a populated `ETY_entry_link` full-scanned per row (no `reflex_id` index) → 2-min timeout; now 15s, fully idempotent (re-run counts identical). Parity proof extended (tregear reflex = entries+cognates; abvd reflex = abvd_cognates) — **PARITY: PASS**, `foreign_key_check` 0, integrity ok. `tests/test_etymology_unified.py` rewritten (16 tests incl. ETY_link/ETY_entry_link FK+shape+origins+legacy-bridge match; `test_proto_key_populated` now allows NULL for abvd). Backup `data/staging_dictionary.db.bak-20260701-154121-s57`. **290 tests all pass.** Staging-only (app-DB `ETY_*` cutover is S59). | ✓ Done | 53, 54, 56 |
| 58 | Walworth gap-fill pass — `build_walworth` added to `52_build_etymology_unified.py` (`walworth` appended to `SOURCES`/`BUILDERS`, MUST build **last** so its novelty test reads the already-projected coverage of the other five sources). **Design:** Walworth cognatesets are bare cognacy classes; a set's reconstructed protoform lives in its **"Proto Polynesian" pseudo-language** form (`walworth_languages.id='Polynesian'`). Only the **202** protoform-bearing sets are candidates — the 1,254 protoform-less sets have no `proto_key` to dedup against, so they can't be judged a gap and are skipped (their attested reflexes only duplicate ABVD's comparative role). A candidate promotes when **(a) novel protoform** — `proto_key` absent from every existing `ETY_cognateset` → whole set + ALL non-proto reflexes; or **(b) novel language** — `proto_key` already exists but it attests a `(proto_key, language)` pair no existing set covers → set with ONLY those novel-language reflexes. Else skipped. Promoted sets+reflexes stamped `gap_fill=1`; the shared `proto_key` lets the existing cross-source dedup in `build_links` link a promoted set to its canonical automatically (no bespoke link code). Walworth Māori reflexes resolve to `lang_key='maori'` (iso `mri`→pollex slug) and enter the entry bridge. **Result: 197 sets promoted (113 novel-protoform + 84 novel-language), 3,418 reflexes; 5 covered/keyless skipped, 1,254 protoform-less never candidates.** Side effects on the full build: `ETY_link` 2,203→2,230 (dedup 315→342, +27 walworth↔canonical); `ETY_entry_link` 114,620→115,564 (+944 walworth links from 77 Māori reflexes via homograph fan-out); **pollex slice still exactly 48,243** (regression guard holds). Parity: PASS (walworth branch = gap-fill subset, `0<promoted≤raw`, not equality), FK-clean, idempotent (re-run identical). New `TestWalworthGapFill` (6 tests: strict-subset, gap_fill stamping both ways, every-set-has-protoform, **every-reflex-novel** `(proto_key,lang)` invariant, reflex FK+source_ref); `walworth` added to `test_entry_link_all_maori_sources`. Backup `data/staging_dictionary.db.bak-20260701-160416-s58`. **296 tests all pass.** Staging-only (app-DB `ETY_*` cutover is S59). | ✓ Done | 55, 57 |
| 59 | Export + hard cutover — the app DB now ships **only** the unified `ETY_*` layer. `scripts/60_export_app_db.py`: `APP_TABLES` adds the six `ETY_*` tables in FK-safe order (`ETY_level`/`ETY_language`/`ETY_cognateset`/`ETY_reflex`/`ETY_link`/`ETY_entry_link` — `entry` already precedes the `ETY_entry_link` FK) and **removes** all nine raw etymology tables (`pollex_cognatesets`/`pollex_reflexes`/`pollex_languages`/`lpo_cognatesets`/`acd_cognatesets`/`etymology_links`/`protoform_ancestry`/`reconstruction_levels`/`pollex_entry_links`) — **no backward-compat views** (safe: new app-DB format not yet shipped). The 12 `idx_ety_*` indexes ride along automatically via the existing `sqlite_master` extras copy. **New: backup** — the prior `maori_dict.db` is copied to `backups/maori_dict_<ts>.db` before the wipe. Export run: 6 `ETY_*` tables shipped (reflex 256,402 / entry_link 115,564 / cognateset 29,823 / link 2,230 / language 2,067 / level 25), 0 raw etym tables present, `integrity_check ok`, `foreign_key_check clean`; app DB **114 MB → 166 MB** (ETY layer is the bulk). New `TestAppDBEtymologyCutover` in `tests/test_etymology_unified.py` (7 tests: ETY shipped, raw etym dropped, no `*_entries` leak, ETY counts == staging, integrity+FK clean, `ETY_entry_link` resolves in-app, FK indexes shipped). Docs: `DATABASE_REFERENCE.md` etymology section rewritten to `ETY_*` (+ old→new migration table, volatile-surrogate warning, updated decomposition queries), `docs/UPDATE_WORKFLOW.md` + `update-dictionary` skill add the `52_build_etymology_unified.py --reset` step + "run before export" rule. **303 tests all pass.** | ✓ Done | 58 |

| 60 | ETY depth-axis normalization + `ETY_depth` labels. **(1) Level-code normalization** — the ACD/LPO comparative backbone wrote raw level codes (`PAN/PMP/POc/PWMP/PPH`, Micronesian + Melanesian subgroups, attested-lang tags) straight into `ETY_cognateset.level`, but only POLLEX's 25 two-letter codes existed in `ETY_level` → **~30k cognate sets had no `depth_rank`** and couldn't sort by antiquity. Added **62 backbone codes** to the `LEVELS` constant in `scripts/pollex_ancestry.py` (→ `reconstruction_levels` → `ETY_level`, rebuild-safe), each mapped onto the existing Māori-lineage ladder (0=Austronesian … 12=Cook Islands Māori); sibling branches anchor at their split point, attested modern languages get NULL rank (excluded from sort). Confidence tagged inline (HIGH on the Māori spine, MED on sibling Oceanic/Micronesian/Melanesian nodes). **`ETY_level` 25 → 90 rows; 0 unmapped non-NULL levels remain.** **(2) `ETY_depth` reference table** — many codes share a `depth_rank`, so grouping picked an arbitrary alphabetical `MIN(name)` (rank 4 showed "Nauruan" not "Remote Oceanic"). New `ETY_depth(depth_rank, label, spine_code)` gives one canonical Māori-spine label per rung (14 rows, 0…12 + 99=Loans/Unknown); seeded rebuild-safe via new `DEPTH_LABELS` constant → staging `depth_labels` → `ETY_depth`. DDL in `00_init_db.py`; `52_build_etymology_unified.py` `build_refs` reseeds it; `60_export_app_db.py` `APP_TABLES` ships it. Live `data/maori_dict.db` + `staging_dictionary.db` patched directly (both DBs gitignored). Docs: `DATABASE_REFERENCE.md` (new `ETY_depth` section + query; `ETY_level` count 25→90) + `SCHEMA_PROPOSAL.md` (ETY_* table list). **Deepest reconstruction = Proto-Austronesian** (rank 0, 2,190 sets); every cognate set across all sources now sorts by antiquity with clean labels. Two commits (`94ef032` normalize, `6717686` ETY_depth) merged to `main` (fast-forward), branch `s59-ety-export-cutover` deleted. Repo has no remote — not pushed. | ✓ Done | 59 |

| 61 | ETY level-code variant reconcile — collapse duplicate spellings of one reconstruction node onto a single canonical code. **Root cause:** `ETY_cognateset.level` copies each source's raw level string verbatim; ACD's CLDF writes all-caps `PAN`/`POC`, LPO tags 2 sets `PSS` — S60's band-aid kept BOTH spellings as separate `reconstruction_levels` rows (`PAN`+`PAn`, `POC`+`POc`, `PSS`+`PSES`) so nothing orphaned on the depth axis, leaving 3 duplicate nodes. **Fix — canonicalize at ingest:** new shared `LEVEL_VARIANT_CANON` map + `canonical_level()` in `scripts/utils.py` (`PAN→PAn`, `POC→POc`, `PSS→PSES`; mixed-case scholarly standard, also the `parent_code` spelling already used in the depth ladder → zero hierarchy edits); `07_acd_import.py` applies it to the parsed level (`PAN`,`POC`), `06_lpo_import.py` applies it to `Level` (`PSS` — an LPO artifact, so ACD-only patch would miss it). Dropped the 3 variant rows from the `LEVELS` constant in `scripts/pollex_ancestry.py` (kept canon, fixed stale `# variant of` comments). **Verified no other emitter** writes the variants (POLLEX cognatesets use only 2-letter Polynesian codes; `protoform_ancestry` ancestors clean) so dropping the reference rows can't orphan. Rebuilt pipeline: `pollex_ancestry.py schema` (**`ETY_level`/`reconstruction_levels` 90→87 rows**) → `07_acd_import` (`PAn` 1771 / `POc` 950) → `06_lpo_import` (`PSS`→`PSES`) → `52_build_etymology_unified.py` (full, **PARITY: PASS**, FK clean) → `60_export_app_db.py` (app `maori_dict.db` 166 MB). **Result:** `ETY_cognateset.level` merged `PAn` 2010 (1771 acd+239 lpo) / `POc` 2030 (950+1080) / `PSES` 14 (12+2); 0 variant leak, 0 duplicate node names in `ETY_level`; app DB verified (87 rows, no leak). Modified: `scripts/utils.py`, `scripts/07_acd_import.py`, `scripts/06_lpo_import.py`, `scripts/pollex_ancestry.py`. Both DBs gitignored. | ✓ Done | 60 |

| 62 | Papakupu `gloss_en` cleanup — strip usage examples out of the gloss. **Root cause:** `50_build_unified.py` `build_papakupu` stored each entry's whole raw `definition` blob (gloss + `<Māori>. <English>. [SRC]` examples + `#[Note from Te Mātāpunenga ©]` editorial blocks) as `sense.gloss_en`, so **2,320 / 4,683 papakupu senses** carried paragraphs of their own example text (e.g. `aroha` gloss = a 1,900-char wall). New shared module `scripts/papakupu_gloss.py` `clean_gloss(definition, example_texts)` returns the definition only, cutting at the earliest of: first `#[Note]` block, first usage-example start, or any stored `example.text_mi` (a backstop guaranteeing 0 leaks). **Hard part = the gloss→example boundary:** the first example's Māori sentence is glued onto the gloss with no separating period (`…sympathy He mate ano…`), and short glosses whose letters are all Māori-valid (`to mouth`, `open`, `narrow`) defeat the naïve letter test the S50 extractor uses — so `example.text_mi` is itself often polluted with the leading gloss, making a naïve `find(text_mi)` over-strip to empty. Detector instead scans for the first **capitalised** word beginning a run of ≥3 alphabetic Māori tokens ending in a sentence terminator, and only treats it as an example boundary when the removed tail actually carries an English-only letter or a `[SRC]` code — this keeps English hapū-census notes full of capitalised Māori place-names (`…Ngapuhi at Kokohuia in 1918.`) intact while still stripping real examples. `build_papakupu` rewired: `gloss_en = clean_gloss(d, [ex.text_mi…])`, `definition_raw` keeps the full raw blob. **Backfill** `scripts/51_papakupu_gloss_backfill.py` (dry-run default + `--apply` + auto-backup) rewrites `sense.gloss_en` in place — **3,649 senses updated, 107 emptied → NULL** (Māori-only example entries; app falls back to `entries.headword_en`); `definition_raw` + the `example` table left byte-for-byte unchanged (10,211 / 10,211). **Verify: papakupu `gloss_en` containing own `example.text_mi` 2,320 → 0** (checked in both staging + app DB). `aroha` → `love, pity, compassion, sympathy`; `mana` → `authority, control, identity, prestige`. App DB re-exported (`60_export_app_db.py`, `maori_dict.db` 164.8 MB, integrity ok, FK clean). New: `scripts/papakupu_gloss.py`, `scripts/51_papakupu_gloss_backfill.py`, `tests/test_papakupu_gloss.py` (9 cases); modified: `scripts/50_build_unified.py`. Backup `data/staging_dictionary.db.bak-20260705-211620`. App repo untouched (fix is in the shared DB the app reads). **324 tests all pass.** | ✓ Done | 50 |

| 63 | TaiKupu new-source import (Ngāpuhi vocab app, Māori Minute platform) — **used with owner's permission**. **Extraction:** the app (`https://maoriminute.com/taikupu-app/daily`) is an Expo/React-Native-Web PWA that fetches its whole dataset from a **public, no-auth JSON API** — `GET https://maoriminute.com/api/dictionary` returns `{version, updatedAt, count, entries:[…]}` (2,265 entries, ~700 KB, one request; `/api/dictionary/version` for cheap change checks). Raw saved to `sources/taikupu/raw/taikupu_dictionary.json` (version `1783144916-867096`). Per-entry: `id, maori, english, example, exampleTranslation, level` (1–100 learning poutama), `notes?`, `levelOverride?`; `partOfSpeech`/`category` present but 100% empty; 774 macron headwords (proper orthography); 102 duplicate headwords are **distinct senses** (e.g. `ao` = the world / to scoop up), not repeats → no dedup. **Own table/schema per design, but shown in-app under the Papakupu banner** (`source_metadata.display_name='Papakupu o Tai Tokerau'`, identical to `papakupu`; `default_dialect='Tai Tokerau'` drives the same dialect boost). New: `taikupu_entries` table + FTS/triggers in `00_init_db.py`, `scripts/40_taikupu_import.py` (`--download`/`--version-check`), `build_taikupu` in `50_build_unified.py` (+`taikupu` in `SOURCES`/`BUILDERS`), `tests/test_taikupu.py` (14 tests). Modified: `35_detect_pairs.py` (taikupu added to `SOURCE_ORDER`/`_QUERIES`; **kept lexicographic** so no existing pair flips — 18,936 new cross-source pairs), tests `test_schema`/`test_unified`/`test_full_suite` (source enumerations + a taikupu dialect test; the "other sources have no dialect" test now excludes both Tai Tokerau sources). **Bug fixed:** the `default_dialect` seed UPDATE ran in `migrate_tables()` *before* `seed_source_metadata()` inserted the new row → moved it after the seed INSERT so brand-new Tai Tokerau sources are tagged on first init. Pipeline: init → import (2,265) → unify (`entry`/`sense`/`example` 2,265 each, dialect=Tai Tokerau) → 08b/52 ETY rebuild (PARITY PASS) → detect_pairs → export (`maori_dict.db` 165.4 MB, integrity ok, FK clean). App DB verified: 2,265 taikupu entries, English glosses, Papakupu banner. Backup `data/staging_dictionary.db.bak-<ts>-taikupu`. **338 tests pass (+3 subtests).** | ✓ Done | 6, 50 |

| 64 | `std_pos` correction + full rebuild/export — user remapped atomic HPK code **`ing`: `Proper noun (person)`/`Tūmoko` → `Noun`/`Tūingoa`** (was mis-set; `ing` is He Pātaka Kupu's main noun code). Ran the std_pos workflow: `16_dump_std_pos_seed.py` (seed 167 rows: reviewed=128, not_pos=39; staged) → full `50_build_unified.py` → 08b → 52 → FTS → pairs → export. **Impact: 11,191 senses raw-POS=`ing` now `Noun`/`Tūingoa`; 23,961 senses carry atomic `ing` in combos; `entry.part_of_speech_en` LIKE '%Noun%' = 80,400.** **Bug found + fixed en route:** full unify crashed `sqlite3.IntegrityError: FOREIGN KEY constraint failed` at `delete_source_slice` `DELETE FROM entry` — `pollex_entry_links` (08b, 49,967 rows) and `ETY_entry_link` (52, 120,003 rows) FK-reference `entry.id` with `foreign_keys=ON`, and `50_build_unified.py` never cleared them (latent since S57/S59; unseen because no *full* unify had run since those tables filled — S63's `--source taikupu` slice had no ETY links). Fix mirrors 52's global-link rule: new `clear_entry_link_tables()` (`ENTRY_LINK_TABLES = pollex_entry_links, ETY_entry_link`) runs in `main()` before the unify loop — entry ids regenerate every rebuild so those links are stale regardless; 08b + 52 rebuild them right after per workflow. Cleared 169,970 stale rows on this run. New `tests/test_build_unified_fk.py` (3 unit tests, in-memory schema: blocked-without-clear repro, unblocked-after-clear, missing-table skip). Rebuild verified: TOTAL entry 108,173 / sense 128,047 (all six sources, counts match S63), scrubbed 20 not_pos senses, 08b 49,967 relinks, 52 PARITY PASS (`ETY_entry_link` 120,003 → 43,198 entries), export `maori_dict.db` **164.7 MB**, integrity ok, FK clean. Modified: `scripts/50_build_unified.py`; new: `tests/test_build_unified_fk.py`; staged: `seeds/std_pos_seed.csv`. **329 tests all pass.** | ✓ Done | 45, 59 |

| 65 | Williams sub-headword split — derivative sub-headwords were glued onto the parent entry's last sense (e.g. `Piri` sense 8 ran 1,896 chars into `piriahi`/`piringa`/`whakapiri`/`pipiri`/`piripiri`; ~1,449 of 11,910 Williams entries affected; the glued words existed as **no searchable headword anywhere**). **Root cause:** `01_williams_parse.py` `parse_section_div` joined **all** direct-child `<p>` of a `div.section` into one definition, but the printed layout packs several entries per div: later `<p class="hang">` paragraphs are further full headwords (e.g. `Pirikahu` inside the Pirihonga div), and paragraph-initial bold lowercase words with a POS/`1.` delimiter are derivative sub-headwords. **Fix — parser group split:** `parse_section_div` now returns a list — paragraphs are partitioned into groups (`kind: main` = first hang, `hang` = later hang headwords, `sub` = bold-lowercase sub-headwords) and each group builds its own entry from only its own paragraphs (`_sub_headword` detector + shared `_build_entry`). Detector covers `<b>piriahi</b>, a.`, multi-sense `<b>whakapiri</b>. <b>1</b>.`, the POS-bolded-with-headword slip `<b>whawhango, a</b>.` (checked **before** the variant-list reading — `a`/`n` look like valid tokens), and span-set sub-heads `<span class="foreign bold">ahatanga</span>, n.`; `part.` added to `POS_RE`. **Id stability (dedup-link protection):** `cross_source_candidates` + the downstream device id reference `williams_entries.id`, so `01_williams_import.py` now assigns explicit deterministic ids — `main` entries keep their positional ids 1–11,910 (parser preserves the main sequence: verified **0/11,910 headword mismatches** vs the pre-fix DB), recovered entries get `1_000_000 + main_id*100 + k` (idempotent across re-runs; e.g. `piriahi` = 1694401 under `Piri` 6944). Id-parity guards: first-hang-without-span divs still skipped whole; a main whose own text was all sub-entries keeps its slot with a synthesized `See …` gloss (2 cases). **Result: 11,910 → 14,905 Williams entries** (+2,919 `sub` +76 `hang` recovered; senses 20,187 → 24,523); `Piri` sense 8 = "The moon on the twenty-third night." (719-char def, was 2,731); `piriahi`/`whakaaio`/`whakapiri`/`whakatina`/`Pirikahu`/`hiahia`-class words now searchable with per-sense POS; run-on-regex last-senses **~1,449 → 30** (residue = mid-paragraph typesetting glue inside a single `<p>`/`<b>`, e.g. `‖ kopi. topitopi` — not paragraph-level splittable, left as-is). Pipeline: backups (`backups/staging_dictionary.db.bak-20260706-215210-williams` + `maori_dict.db.bak-…`) → parse → import → `50 --source williams` → 08b (50,581 links) → 52 (**PARITY PASS**) → FTS → detect_pairs (existing pairs untouched) → export `maori_dict.db` **166.1 MB**, integrity ok, FK clean. Modified: `scripts/01_williams_parse.py`, `scripts/01_williams_import.py`. **329 tests all pass.** | ✓ Done | 3, 4 |

| 66 | Williams mid-paragraph glued sub-headword recovery — S65's group split only caught sub-heads that already headed their own `<p>`; 38 more sub-headwords were still glued **mid-paragraph** behind a parent's prose (e.g. `whakahinuhinu` inside `Hinu`'s paragraph, `rainga` inside `Rai`'s), invisible to search and still hanging off the parent's last sense. **Fix — kinship-guarded mid-paragraph splitter (`01_williams_parse.py`):** new `_split_midparagraph` walks a paragraph's runs and splits after a sentence-final boundary when the next bold/span run is (a) lowercase-with-POS and (b) morphologically related to the section's headword (`_related`) — guards against unrelated bolded words or bare cross-refs (`= <b>titoki</b>.`) triggering a false split. New detectors: plain-text sub-heads with no bolding at all (`PLAIN_SUBHEAD_RE`, bucket C, e.g. `rainga, n. Undulation.`) and POS+sense-number packed into one bold run (bucket D, e.g. `<b>māwhitiwhiti, n. 1</b>.`). Emitted as new `kind="subx"` (distinct from S65's `sub`/`hang`) so downstream id assignment can band them separately. **Accepted-residue guard:** paragraphs containing a mid-paragraph capitalized roman-numeral homonym restart (e.g. `Hou (vi) = hoi, a. Distant.` inside `houhou`'s paragraph) are never split (commit `034cf79`) — attribution of text after such a restart is unknowable — leaving exactly 1 accepted residue (`houhou`, id 1146001). Bucket breakdown of the 38 recovered (pre-fix-round count; see final-review correction below): **A=26** (mid-paragraph bold after a sentence boundary), **B=7** (multiple heads packed into one bold run, e.g. `kopi. topitopi`), **C=4** (plain-text, no bold), **D=1** (POS+number-in-bold). **Id stability (`01_williams_import.py`):** extracted the id loop into module-level `assign_ids(entries)`; `subx` entries take `1_000_000 + main_id*100 + 50 + j` (`j` 1-based per main, cap 49 — S65's `sub`/`hang` band tightened from k=1..99 to k=1..49 to make room, current max per parent is 6 so no regression risk) — existing `main`/`sub`/`hang` ids are untouched (snapshot diff: **`OK: 0 changed, 38 added`**, all new ids in the k≥51 band). New `TestImportBandIds` unit test (TDD RED→GREEN) + 2 new end-to-end parse tests (`TestParseSectionDivSubx`) + 9 splitter/detector unit tests in `tests/test_williams_subsplit.py`. **Result: 14,905 → 14,943 Williams entries** (2,995 S65 sub/hang + 38 S66 subx; senses 24,523 → 24,558); residue check on last-sense run-on regex: **1 hit** (`houhou`, accepted); spot-checked recovered glosses present (`whakahinuhinu`/Glossy, `rainga`/Undulation, `topitopi`/Alectryon, `tohunga`/Skilled person). Pipeline: backups (`backups/staging_dictionary.db.bak-20260707-135454-williams-subx` + `maori_dict.db.bak-…`) → parse → import → `50 --source williams` (entry 14,943) → 08b (50,587 links) → 52 (**PARITY PASS**, `ETY_entry_link` 122,950 → 44,865 entries) → FTS (`williams_fts` 14,943 rows) → detect_pairs (109 new, 213,860 pre-existing skipped) → export `maori_dict.db` **166.1 MB**, integrity ok, FK clean. Deleted the frozen `sources/williams/parsed/williams_entries.golden.json` (S65 baseline, now superseded). Modified: `scripts/01_williams_import.py`; new tests in `tests/test_williams_subsplit.py`. **347 tests all pass.**<br>**Final-review fix round (same session, post-landing):** two bugs found in code review, both in `_related()`'s kinship guard / the bucket-D group-payload assembly, both TDD RED→GREEN. (1) **`nawai` false-positive split:** `_related("nawai", "Nāwai")` returned `True` on raw identity alone (`normalise_search_key()` maps both to the same key — trivially satisfies the old `a in b or b in a` test), so a Taranaki dialect-cognate citation (`‖ Ta. nawai, v., hold out, last.`, inside `Nāwai`'s own paragraph) was misread as a derivative sub-headword and split into a spurious entry (leaked-comma gloss `", hold out, last."`, id `1542251`). Fix: `_related()` now rejects candidate/parent pairs whose **raw** (un-whaka-stripped) search keys are equal, checked *before* `_strip_whaka` so genuine derivatives that become equal only *after* whaka-stripping (`whakawari`/`Wari`) still pass — 2 new `TestRelated` cases + a full-paragraph `_split_midparagraph` repro pin this. The citation text is now back inside `Nāwai`'s own definition, as printed. (2) **`māwhitiwhiti` bucket-D gloss leak:** `_group_payload()` only read `elem.tail` for a sub-head's head_tail, missing the POS+sense-number fused *inside* the bold run itself (`<b>māwhitiwhiti, n. 1</b>`) — leaked into `definition` as `"n. 1. Grasshopper…"` with `part_of_speech` empty. Fixed narrowly (opt-in `fused_sense_num` param on `_build_entry`, populated only when `_group_payload` detects the exact bucket-D bold shape) so the ubiquitous *ordinary* "POS then a plain-text `1.` sense marker" convention used by hundreds of other entries (e.g. `Tākoko`, `Tāngaengae`) is untouched: `part_of_speech` now `"n."`, `sense_number` `"1"`, `definition` starts `"Grasshopper. 2. A marine crustacean…"`. Parse-stage JSON diff against the pre-fix baseline confirmed **exactly** these 2 entries changed plus the 1 removal (`nawai`) — nothing else moved. **Result: 14,943 → 14,942 Williams entries, subx 38 → 37** (bucket breakdown revised **A=25/B=7/C=4/D=1**; `nawai` was bucket A). Re-ran full pipeline: id-check vs `backups/williams_ids_pre_subx.json` → `OK: 0 changed, 37 added, all in k>=51 band` (id `1542251` gone); `50 --source williams` → entry 14,942/sense 24,556/relation 4,117; 08b 50,587 links; 52 **PARITY PASS**; FTS `williams_fts` 14,942 rows; detect_pairs 0 new (213,967 pre-existing skipped); export `maori_dict.db` **166.1 MB**, integrity ok, FK clean (unified `entry` 111,205, `sense` 132,416, `relation` 107,870 — both down from S66 by exactly the `nawai` removal + `māwhitiwhiti`'s now-single merged sense, an expected consequence of `split_senses()` no longer finding a valid 1,2,… marker run once the leaked `n. 1.` prefix is gone). Residue check unchanged: **1 hit** (`houhou`, accepted). Full suite **352 tests pass** (5 new: 2 `TestRelated`, 1 `_split_midparagraph` nawai-citation repro, 1 pm-branch splitter test, 1 `TestParseSectionDivSubx` māwhitiwhiti end-to-end). Modified: `scripts/01_williams_parse.py`; new tests in `tests/test_williams_subsplit.py`. | ✓ Done | 65 |

**Summary:** 66 done (incl. 33–51, 52–66), 0 in progress, 2 skipped (25c, 26), 0 planned. Last updated 2026-07-07 (S66 landed + same-session final-review fix round: recovered 38 mid-paragraph glued Williams sub-headwords the S65 group-split missed [buckets A/B/C/D; kinship-guarded `_split_midparagraph` + plain-text/POS-in-bold detectors; new `kind="subx"`], then code review caught two bugs in that same kinship guard: an identity false-positive (`_related()` trivially passed on raw `normalise_search_key()` equality, misreading the Taranaki citation `‖ Ta. nawai` as a derivative of its own parent `Nāwai` and splitting it into a spurious entry) and a bucket-D gloss leak (`māwhitiwhiti`'s fused `<b>…, n. 1</b>` POS+sense-number leaking into its definition). Both fixed TDD RED→GREEN, narrowly scoped (raw-key identity reject in `_related()`; opt-in `fused_sense_num` param so the fix touches only the one bucket-D shape, not the hundreds of ordinary entries with a plain-text `1.` sense marker). Parse-stage JSON diff proved exactly 2 entries changed + 1 removed, nothing else. **Final: 14,905 → 14,942 Williams entries** (subx 37, bucket breakdown A=25/B=7/C=4/D=1); `assign_ids()` bands `subx` at `1e6+main*100+50+j`, id-stability diff `OK: 0 changed, 37 added`; app DB re-exported 166.1 MB (unified entry 111,205 / sense 132,416 / relation 107,870); **352 tests pass**). **Prior:** 65 done (incl. 33–51, 52–65), 0 in progress, 2 skipped (25c, 26), 0 planned. Last updated 2026-07-07 (S65 landed: Williams parser now splits derivative sub-headwords/extra hang headwords into their own entries [11,910 → 14,905 entries, +2,995 recovered; deterministic ids `1e6+parent*100+k`, mains keep 1–11,910 so dedup links survive]; `Piri` sense 8 clean; run-on last-senses ~1,449 → 30; app DB re-exported 166.1 MB; **329 tests pass**). **Prior:** 64 done (incl. 33–51, 52–64), 0 in progress, 2 skipped (25c, 26), 0 planned. Last updated 2026-07-06 (S64 landed: `std_pos` `ing` remapped `Proper noun (person)`→`Noun`/`Tūingoa` [11,191 exact senses, 23,961 in combos]; fixed latent full-rebuild FK crash by clearing `pollex_entry_links`/`ETY_entry_link` before unify [new `clear_entry_link_tables()` + 3 unit tests]; full pipeline re-run, app DB re-exported 164.7 MB; **329 tests pass**). **Prior:** 63 done (incl. 33–51, 52–63), 0 in progress, 2 skipped (25c, 26), 0 planned. Last updated 2026-07-05 (S63 landed: new TaiKupu source imported from Māori Minute's public JSON API [2,265 Ngāpuhi vocab entries, used with owner permission] into its own `taikupu_entries` table/FTS + `40_taikupu_import.py` + `build_taikupu` unify + `test_taikupu.py`; shown in-app under the Papakupu banner [shared display_name + Tai Tokerau dialect]; fixed default_dialect seed-order bug; 18,936 new cross-source pairs; app DB re-exported; **338 tests pass (+3 subtests)**). **Prior:** 62 done (incl. 33–51, 52–62), 0 in progress, 2 skipped (25c, 26), 0 planned. Last updated 2026-07-05 (S62 landed: stripped usage examples out of papakupu `sense.gloss_en` — new `papakupu_gloss.clean_gloss` [capital-Māori-run boundary detector + `text_mi` backstop] wired into `build_papakupu`; backfill rewrote 3,649 glosses [107→NULL], `example` table untouched; **gloss_en-contains-own-example-text_mi 2,320 → 0**; app DB re-exported; 324 tests pass). **Prior:** 61 done (incl. 33–51, 52–61), 0 in progress, 2 skipped (25c, 26), 0 planned. Last updated 2026-07-05 (S61 landed: collapsed 3 duplicate ETY level-code spellings [`PAN→PAn`, `POC→POc`, `PSS→PSES`] via a shared `canonical_level()` applied at ACD+LPO ingest; dropped the 3 band-aid variant rows from `pollex_ancestry.py` `LEVELS` [`ETY_level` 90→87]; full ETY rebuild PARITY PASS; app DB re-exported + verified no variant leak). **Prior:** 60 done (incl. 33–51, 52–60), 0 in progress, 2 skipped (25c, 26), 0 planned. **Etymology unification (`ETY_*`, sessions 52–59) COMPLETE**; S60 added the cross-source depth axis + labels. Last updated 2026-07-05 (S60 landed: normalized 62 ACD/LPO backbone level codes onto `ETY_level.depth_rank` [25→90 rows, 0 unmapped]; new `ETY_depth` per-rank label table [14 rows]; both DBs patched + docs synced; commits `94ef032`+`6717686` merged to `main`). **Prior:** Last updated 2026-07-01 (S59 landed: app-DB hard cutover — `60_export_app_db.py` now ships only the six `ETY_*` tables and drops all nine raw per-source etymology tables from the app surface with no compat views; backup of the prior `maori_dict.db` written to `backups/` before wipe; export integrity+FK clean, app DB 114→166 MB; `DATABASE_REFERENCE.md`/`UPDATE_WORKFLOW.md`/`update-dictionary` skill updated for the `ETY_*` surface + the `52_build_etymology_unified.py`-before-export rule; new `TestAppDBEtymologyCutover` [7 tests]; 303 tests pass). Next: no etymology work outstanding.

**Earlier summary:** 52 done (incl. 33–51), 0 in progress, 2 skipped (25c, 26). Last updated 2026-06-30 (session 51 closed: etymology linking missed-opportunity sweep — new `pollex_entry_links` reverse bridge 48,243 links; `etymology_links` 229→421 via Polynesian-level mapping + macron folding + Tier-3 bare citations; 148 tests pass).
